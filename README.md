# fgacic (Vite + React)

Centered single-screen UI. Commands: `npm install`, `npm run dev`, `npm run build`.

Uses Vite 5 for compatibility with Node 20.x and 21.x (Vite 8 expects Node 20.19+ or 22.12+).
